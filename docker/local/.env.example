# Copie para docker/local/.env e ajuste se necessário:
#   cp docker/local/.env.example docker/local/.env

POSTGRES_DB=speed_violation
POSTGRES_USER=speed_violation
POSTGRES_PASSWORD=speed_violation
POSTGRES_PORT=5433
SERVER_PORT=8080
