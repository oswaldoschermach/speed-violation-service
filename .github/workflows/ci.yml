name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  test:
    name: Test & coverage (mvnw verify)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run tests, integration and coverage gate
        run: chmod +x mvnw && ./mvnw -B --no-transfer-progress verify

      - name: Upload JaCoCo report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco
          if-no-files-found: ignore

  build-and-push:
    name: Build & push image (GHCR)
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short
            type=semver,pattern={{version}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/prod/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to VPS (SSH)
    needs: build-and-push
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH (git pull + compose pull + up)
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -euo pipefail
            cd "${{ vars.DEPLOY_PATH }}"
            git pull --ff-only
            if docker compose version >/dev/null 2>&1; then
              DC="docker compose"
            else
              DC="docker-compose"
            fi
            COMPOSE="-f docker/prod/compose.yaml --env-file docker/prod/.env"
            # docker-compose v1 bug: "Recreating" after pull → KeyError ContainerConfig.
            # Pull/replace only app; ensure postgres/caddy are up (first failed deploy may have stopped postgres).
            if ! docker inspect -f '{{.State.Running}}' speed-violation-postgres-prod 2>/dev/null | grep -q true; then
              docker ps -aq --filter "name=speed-violation-postgres" | xargs -r docker rm -f
              $DC $COMPOSE up -d postgres
              for i in $(seq 1 30); do
                if docker inspect -f '{{.State.Health.Status}}' speed-violation-postgres-prod 2>/dev/null | grep -q healthy; then
                  break
                fi
                sleep 2
              done
            fi
            $DC $COMPOSE up -d --no-recreate caddy 2>/dev/null || $DC $COMPOSE up -d caddy
            $DC $COMPOSE pull app
            docker rm -f speed-violation-app-prod 2>/dev/null || true
            $DC $COMPOSE up -d --no-deps app
            for i in $(seq 1 45); do
              if docker inspect -f '{{.State.Health.Status}}' speed-violation-app-prod 2>/dev/null | grep -q healthy; then
                break
              fi
              sleep 2
            done
            curl -fsS http://127.0.0.1:8080/actuator/health/readiness
            docker image prune -f
